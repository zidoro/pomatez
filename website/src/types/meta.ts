export type SiteMetaProps = {
  site: {
    siteMetadata: {
      title: string;
      description: string;
      author: string;
      siteUrl: string;
      twitterUsername: string;
      googleVerification: string;
    };
  };
};
