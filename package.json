{
  "name": "timeframe-app",
  "version": "1.0.0",
  "private": true,
  "main": "public/electron.js",
  "description": "Time Management Application inspired by Pomodoro Technique",
  "author": {
    "name": "Roldan Montilla Jr",
    "email": "roldanjrmontilla@gmail.com",
    "url": "https://github.com/roldanjrCodeArts9711"
  },
  "scripts": {
    "react-start": "react-scripts start",
    "react-build": "react-scripts build",
    "postinstall": "electron-builder install-app-deps",
    "electron-build": "electron-builder --windows --linux",
    "electron-release": "electron-builder --windows --linux --publish always",
    "clean-build": "rm -rf build/ dist/",
    "start": "concurrently \"cross-env BROWSER=none yarn react-start\" \"wait-on http://localhost:3000 && electron . \"",
    "build": "yarn clean-build && yarn react-build && yarn electron-build"
  },
  "build": {
    "appId": "com.roldanjrCodeArts9711.TimeframeApp",
    "productName": "Timeframe App",
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "deb",
      "category": "Production"
    }
  },
  "dependencies": {
    "electron-is-dev": "^1.1.0",
    "node-sass": "^4.12.0",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-ripples": "^2.2.1",
    "react-scripts": "3.2.0",
    "react-spring": "^8.0.27"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "cross-env": "^6.0.3",
    "electron": "^6.0.12",
    "electron-builder": "^21.2.0",
    "wait-on": "^3.3.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
