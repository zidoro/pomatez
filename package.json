{
  "name": "pomatez",
  "version": "1.2.2",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --output-logs=new-only --filter=@pomatez/website",
    "build": "turbo run build --output-logs=new-only --filter=@pomatez/website",
    "preview": "turbo run preview --output-logs=new-only --filter=@pomatez/website",
    "lint": "turbo run lint --output-logs=new-only --filter=@pomatez/website",
    "gen:tts": "lerna run gen:tts --stream",
    "format": "prettier --write .",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix"
    ],
    "*.{md,json,css,scss,sass}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@pomatez/eslint-config": "*",
    "lint-staged": "latest",
    "prettier": "latest",
    "turbo": "latest",
    "typescript": "^4.9.5",
    "husky": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "yarn@1.22.19"
}
