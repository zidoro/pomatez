{
  "name": "productivity-timer",
  "version": "1.0.1",
  "private": true,
  "main": "public/electron.js",
  "description": "Time Management App for Productivity inspired by Pomodoro Technique.",
  "keywords": [
    "Productivity Timer Application",
    "Time Management Application",
    "Pomodoro Timer Application",
    "Tomato Timer Application"
  ],
  "author": {
    "name": "Roldan Montilla Jr",
    "email": "roldanjrmontilla@gmail.com",
    "url": "https://github.com/roldanjrCodeArts9711"
  },
  "scripts": {
    "react-start": "react-scripts start",
    "react-build": "react-scripts build",
    "postinstall": "electron-builder install-app-deps",
    "electron-build": "electron-builder --windows",
    "electron-release": "electron-builder --windows --publish always",
    "clean-build": "rm -rf build/ dist/",
    "start": "concurrently \"cross-env BROWSER=none yarn react-start\" \"wait-on http://localhost:3000 && electron . \"",
    "build": "yarn clean-build && yarn react-build && yarn electron-build",
    "release": "yarn clean-build && yarn react-build && yarn electron-release"
  },
  "homepage": "./",
  "dependencies": {
    "electron-updater": "^4.1.2",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "^3.2.0",
    "react-spring": "^8.0.27",
    "say": "^0.16.0",
    "v8-compile-cache": "^2.1.0"
  },
  "devDependencies": {
    "@loadable/component": "^5.10.3",
    "concurrently": "^5.0.0",
    "cross-env": "^6.0.3",
    "electron": "^7.0.0",
    "electron-builder": "^21.2.0",
    "node-sass": "^4.13.0",
    "react-ripples": "^2.2.1",
    "wait-on": "^3.3.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
